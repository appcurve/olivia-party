// hook that calls the provided callback function at a regular interval
export * from './lib/use-interval/useInterval'

// joysticks + gamepads via Gamepad API
export * from './lib/use-joystick/useJoystick'

// keyboard "gaming keys" keyup + keydown via keyboard events
export * from './lib/use-keyboard/useKeyboard'

// speech via Speech Synthesis API (text-to-speech)
export * from './lib/use-speech/SpeechContextProvider'
export * from './lib/use-speech/useSpeech'
